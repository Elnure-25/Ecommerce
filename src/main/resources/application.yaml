spring:
  application:
    name: ecommerce

  datasource:
    url: jdbc:postgresql://localhost:5433/ecommerce_db
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  flyway:
    baseline-on-migrate: true